// Phase 2 stubs — company tracking

export async function companySearch(_query: string): Promise<void> {
  console.log('[Phase 2 — not yet implemented] company search:', _query);
}

export async function companyFollow(_companyUrl: string): Promise<void> {
  console.log('[Phase 2 — not yet implemented] company follow:', _companyUrl);
}
